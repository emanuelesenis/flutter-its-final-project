import 'package:flutter/material.dart';

class AltraPagina extends StatelessWidget {
  const AltraPagina({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Benvenuto", style: TextStyle(fontSize: 30))),
    );
  }
}
